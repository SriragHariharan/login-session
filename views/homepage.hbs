<div class="container">
    <div class="d-flex justify-content-between my-3">
        <h1>Hello {{username}} 👋</h1>
        <a href="/logout" class="btn btn-transparent border border-3">🔴Logout</a>
    </div>
    <hr>
    <div class="text-center text-primary my-5 h1">Products Gallery</div>
    
    {{!-- products list --}}
    <div class="row">
        {{#each products}}
        <div class="col col-6 col-md-3">
            <div class="card p-3" style="width: 20rem; height:32rem; margin:10px;">
                <img src={{image}} class="card-img-top" alt={{title}} style="height: 22rem;">
                <div class="card-body">
                    <h6 class="card-title">{{title}}</h6>
                    <p class="card-text">₹ {{price}}</p>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>